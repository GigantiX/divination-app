/**
 * OrderOnline now lives in Apps > Participant Attendance. This compatibility
 * component prevents the legacy report form from rendering a second entry
 * point while existing client bundles are refreshed.
 */
export function OrderOnlineConnect(_props: { batchId: string }) {
    return null
}
